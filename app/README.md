# Sideline app (Pear)

Phase 2 target: the desktop/mobile client.

- Pear runtime app (`pear run`), UI for fan and vendor modes
- Wallet + tab loading via Tether WDK (`@wdk/wallet-evm`)
- Offline voucher handoff over Hyperswarm LAN discovery (no WAN needed)
- QR fallback for zero-radio environments

Not yet implemented — see the roadmap in the root README.
