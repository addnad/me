{
  "name": "@sideline/contracts",
  "version": "0.1.0",
  "description": "SidelineEscrow — prefunded bearer-voucher escrow for offline USDT payments",
  "license": "MIT",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "@sideline/voucher": "*"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "solc": "^0.8.28"
  }
}
